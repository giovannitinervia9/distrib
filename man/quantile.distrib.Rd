% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numerical_functions.R
\name{quantile.distrib}
\alias{quantile.distrib}
\title{Quantile Function for 'distrib' Objects}
\usage{
\method{quantile}{distrib}(x, p, theta, lower.tail = TRUE, log.p = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{"distrib"}.}

\item{p}{A vector of probabilities.}

\item{theta}{A named list of parameters. Vectors are supported (batch calculation).}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le x)}, otherwise \eqn{P(X > x)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities \code{p} are given as \code{log(p)}.}

\item{...}{Additional arguments passed to the internal CDF numerical engines.}
}
\value{
A numeric vector of quantiles.
}
\description{
Computes the Quantile Function (Inverse CDF) for a custom distribution object.
Since most custom distributions do not have an analytical quantile function,
this function employs numerical methods to invert the Cumulative Distribution Function (CDF).
}
\details{
\strong{Numerical Strategy:}
\itemize{
\item \strong{Continuous Distributions:} Uses \code{\link[stats]{uniroot}} to find the root of \eqn{CDF(q) - p = 0}.
It automatically expands the search interval if the root is not bracketed by the initial guess.
\item \strong{Discrete Distributions:}
\enumerate{
\item \strong{Geometric Expansion:} Finds an interval \eqn{[low, high]} containing the quantile by exponentially
expanding a search window around a starting guess (Mean or Mode).
\item \strong{Bisection:} Performs a binary search within that interval to find the smallest integer \eqn{k}
such that \eqn{CDF(k) \ge p}.
}
}
}
